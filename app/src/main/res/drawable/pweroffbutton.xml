<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Mendefinisikan powerbutton saat di klik -->
    <item android:drawable="@drawable/poweroffx"
        android:state_pressed="true" />

    <!-- Mendefinisikan powerbutton saat terfokus -->
    <item android:drawable="@drawable/poweroffx"
        android:state_focused="true" />

    <!-- Mendefinisikan powerbutton saat dalam kondisi normal -->
    <item android:drawable="@drawable/poweroff" />
</selector>